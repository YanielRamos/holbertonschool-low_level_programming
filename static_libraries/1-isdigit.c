#include "main.h"

/**
 * _isdigit - starting point
 * Return: 0 Succesful
 * @c: variable
 */

int _isdigit(int c)
{
	if (c >= 48 && c <= 57)
	{
		return (1);
	}
	else
	{
		return (0);
	}
	return (0);
}
